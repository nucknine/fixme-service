# docker compose up -d redis mongodb mysql postgres rabbitmq elasticsearch kibana
name: "dictapp"
services:
  redis:
    container_name: redis_container
    image: redis:alpine
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --loglevel warning
    volumes:
      - ./docker-volumes/cache:/data
    networks:
      - elastic
  mongodb:
    container_name: mongodb_container
    image: mongo:latest
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - ./docker-volumes/data:/data/db
    networks:
      - elastic
  mysql:
    container_name: mysql_container
    image: mysql:latest
    restart: always
    #command: --default-authentication-plugin-mysql_native_password
    environment:
      - MYSQL_USER=dict
      - MYSQL_DATABASE=dict_auth
      - MYSQL_ROOT_PASSWORD=api
      - MYSQL_PASSWORD=api
    ports:
      - "3306:3306"
    volumes:
      - ./docker-volumes/mysql:/var/lib/mysql
    networks:
      - elastic
  postgres:
    container_name: postgres_container
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=dict
      - POSTGRES_PASSWORD=api
      - POSTGRES_DB=dict_reviews
    ports:
      - "5432:5432"
    volumes:
      - ./docker-volumes/postgres:/var/lib/postgresql/data
    networks:
      - elastic
  rabbitmq:
    container_name: rabbitmq_container
    image: rabbitmq:3.13.7-management-alpine
    restart: always
    environment:
      - RABBITMQ_DEFAULT_USER=fixme
      - RABBITMQ_DEFAULT_PASS=fixmepass
    ports:
      # AMQP
      - "5672:5672"
      # Management UI dashboard
      - "15672:15672"
    networks:
      - elastic
  elasticsearch:
    container_name: elasticsearch_container
    image: docker.elastic.co/elasticsearch/elasticsearch-wolfi:9.0.0
    restart: always
    user: "1000:1000"
    init: true
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      ES_JAVA_OPTS: -Xmx1g -Xms1g
      bootstrap.memory_lock: "true"
      discovery.type: single-node
      network.host: 0.0.0.0
      transport.host: 127.0.0.1
      http.host: 0.0.0.0
      xpack.security.enabled: "true"
      xpack.security.authc.api_key.enabled: "true"
      xpack.security.enrollment.enabled: "true"
      xpack.monitoring.collection.enabled: "false"
      xpack.security.authc.token.enabled: "true"
      cluster.routing.allocation.disk.watermark.low: 95%
      cluster.routing.allocation.disk.watermark.high: 97%
      cluster.routing.allocation.disk.watermark.flood_stage: 99%
      # basic ELASTIC USER NAME - elastic
      ELASTIC_PASSWORD: admin1234
      KIBANA_PASSWORD: kibana
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - ./docker-volumes/elasticsearch:/usr/share/elasticsearch/data
      # - ELASTIC_PASSWORD=dict
      # - ELASTIC_PASSWORD=api
    networks:
      - elastic
    # If the command fails (non-zero exit code), Docker marks the container as unhealthy
    # The current healthcheck only checks the return code, not the actual status content
    # For most cases, the simple version is sufficient
    healthcheck:
      # curl -u elastic:admin1234 - Basic authentication (Username: elastic, Password: admin1234)
      # Sends Authorization: Basic <base64-encoded-credentials> header
      test:
        [
          "CMD-SHELL",
          "curl -u elastic:admin1234 http://localhost:9200/_cluster/health || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
  kibana:
    container_name: kibana_container
    image: docker.elastic.co/kibana/kibana:9.0.0
    restart: always
    ports:
      - 5601:5601
    environment:
      - ELASTICSEARCH_HOSTS=["http://elasticsearch_container:9200"]
      - ELASTICSEARCH_USERNAME=kibana_system
      - ELASTICSEARCH_PASSWORD=kibana
      # docker compose stop elasticsearch && docker compose rm -f elasticsearch will reset the service token
      - ELASTICSEARCH_SERVICEACCOUNT_TOKEN=AAEAAWVsYXN0aWMva2liYW5hL2ZpeG1lLWtpYmFuYTp0bjBzTk5oV1JGZXJhNVFfaEFubllB
      - XPACK_FLEET_AGENTS_ELASTICSEARCH_HOSTS=["http://elasticsearch_container:9200"]
    networks:
      - elastic
    volumes:
      - ./kibana.yml:/usr/share/kibana/config/kibana.yml:ro
    depends_on:
      - elasticsearch

  metricbeat:
    container_name: metricbeat_container
    image: docker.elastic.co/beats/metricbeat:9.0.0
    user: root
    restart: always
    ports:
      - 5066:5066
    networks:
      - elastic
    volumes:
      # Metricbeat config file, mounted read-only so the container can't modify it
      - ./metricbeat.yml:/usr/share/metricbeat/metricbeat.yml:ro
      # Docker socket — lets metricbeat collect container metrics (docker module)
      - /var/run/docker.sock:/var/run/docker.sock:ro
    # --strict.perms=false: skip config file permission checks (needed because the file is mounted from host)
    # -system.hostfs=/hostfs: tell metricbeat the host filesystem is mounted at /hostfs (so it reads host metrics, not container metrics)
    command: ["--strict.perms=false"]
    depends_on:
      - elasticsearch

  heartbeat:
    container_name: heartbeat_container
    image: docker.elastic.co/beats/heartbeat:9.0.0
    restart: always
    user: root
    hostname: heartbeat
    # Grant NET_RAW capability so heartbeat can perform ICMP (ping) checks
    # Note: Docker CLI uses --cap-add (hyphen), but Compose uses cap_add (underscore)
    cap_add:
      - NET_RAW
    # Use host network so heartbeat can reach services running on the host via localhost
    # This is needed on WSL2 where host.docker.internal doesn't route to the WSL2 VM
    # network_mode: host
    # extra_hosts:
      # Allow reaching containers by name (elasticsearch) via the Docker DNS
      # - "elasticsearch_container:127.0.0.1"
    volumes:
      - "./heartbeat.yml:/usr/share/heartbeat/heartbeat.yml:ro"
    command: ["--strict.perms=false"]
    depends_on:
      - elasticsearch

  apm-server:
    container_name: apm_server_container
    image: docker.elastic.co/apm/apm-server:9.0.0
    restart: always
    user: apm-server
    ports:
      - 8200:8200
    networks:
      - elastic
    volumes:
      - "./apm-server.yml:/usr/share/apm-server/apm-server.yml:ro"
    command: ["--strict.perms=false", "-e"]
    # -e: log to stderr instead of file
    # --strict.perms=false: skip config file permission checks (needed for volume-mounted configs)
    depends_on:
      elasticsearch:
        condition: service_healthy

  notifications:
    container_name: notification_container
    build:
      context: ../server/2-notification-service
      dockerfile: Dockerfile.dev
      args:
        NPM_TOKEN: ${NPM_TOKEN}
    restart: always
    ports:
      - 4001:4001
    env_file: ../server/2-notification-service/.env
    environment:
      - ENABLE_APM=0
      - NODE_ENV=development
      - CLIENT_URL=http://localhost:3000
      - RABBITMQ_ENDPOINT=amqp://fixme:fixmepass@rabbitmq_container:5672
      - SENDER_EMAIL=adaline.farrell@ethereal.email
      - SENDER_EMAIL_PASSWORD=mr2zUZB1cHe9TWvF2y
      - ELASTIC_SEARCH_URL=http://elastic:admin1234@elasticsearch_container:9200
      - ELASTIC_APM_SERVER_URL=http://localhost:8200
      - ELASTIC_APM_SECRET_TOKEN=
    depends_on:
      elasticsearch:
        condition: service_healthy
      rabbitmq:
        condition: service_started
    networks:
      - elastic
  gateway:
    container_name: gateway_container
    build:
      context: ../server/1-gateway-service
      dockerfile: Dockerfile.dev
      args:
        NPM_TOKEN: ${NPM_TOKEN}
    restart: always
    ports:
      - 4000:4000
    env_file: ../server/1-gateway-service/.env
    environment:
      - ENABLE_APM=0
      - GATEWAY_JWT_TOKEN=3f472800bcd6467b4d3b121d054e57e3
      - JWT_TOKEN=84f8e8de359784c77e032ceb2cb84df8
      - NODE_ENV=development
      - SECRET_KEY_ONE=bbe654fb66cdc50651355f08b5af52ff
      - SECRET_KEY_TWO=034c7c2ebccecc0fbe0703dc4ae9d057
      - CLIENT_URL=http://localhost:3000
      - AUTH_BASE_URL=http://auth_container:4002
      - USERS_BASE_URL=http://localhost:4003
      - GIG_BASE_URL=http://localhost:4004
      - MESSAGE_BASE_URL=http://localhost:4005
      - ORDER_BASE_URL=http://localhost:4006
      - REVIEW_BASE_URL=http://localhost:4007
      - REDIS_HOST=redis://redis_container:6379
      - ELASTIC_SEARCH_URL=http://elastic:admin1234@elasticsearch_container:9200
      - ELASTIC_APM_SERVER_URL=http://localhost:8200
      - ELASTIC_APM_SECRET_TOKEN=
    depends_on:
      elasticsearch:
        condition: service_healthy
      rabbitmq:
        condition: service_started
    networks:
      - elastic

  auth:
    container_name: auth_container
    build:
      context: ../server/3-auth-service
      dockerfile: Dockerfile.dev
    restart: always
    ports:
      - 4002:4002
    env_file: ../server/3-auth-service/.env
    environment:
      - ENABLE_APM=1
      - GATEWAY_JWT_TOKEN=3f472800bcd6467b4d3b121d054e57e3
      - JWT_TOKEN=84f8e8de359784c77e032ceb2cb84df8
      - NODE_ENV=development
      - API_GATEWAY_URL=http://gateway_container:4000
      - CLIENT_URL=http://localhost:3000
      - RABBITMQ_ENDPOINT=amqp://fixme:fixmepass@rabbitmq_container:5672
      - MYSQL_DB=mysql://dict:api@mysql_container:3306/dict_auth
      - CLOUD_NAME=dwg9ek7pv
      - CLOUD_API_KEY=373746247683531
      - CLOUD_API_SECRET=-iQodxUen3OEQ3UwMd411JTwQpDw
      - ELASTIC_SEARCH_URL=http://elastic:admin1234@elasticsearch_container:9200
      - ELASTIC_APM_SERVER_URL=http://localhost:8200
      - ELASTIC_APM_SECRET_TOKEN=
    depends_on:
      - elasticsearch
      - mysql
    networks:
      - elastic

  users:
    container_name: users_container
    build:
      context: ../server/4-users-service
      dockerfile: Dockerfile.dev
    restart: always
    ports:
      - 4003:4003
    env_file: ../server/4-users-service/.env
    environment:
      - ENABLE_APM=1
      - GATEWAY_JWT_TOKEN=3f472800bcd6467b4d3b121d054e57e3
      - JWT_TOKEN=84f8e8de359784c77e032ceb2cb84df8
      - NODE_ENV=development
      - API_GATEWAY_URL=http://gateway_container:4000
      - CLIENT_URL=http://localhost:3000
      - RABBITMQ_ENDPOINT=amqp://fixme:fixmepass@rabbitmq_container:5672
      - CLOUD_NAME=dwg9ek7pv
      - CLOUD_API_KEY=373746247683531
      - CLOUD_API_SECRET=-iQodxUen3OEQ3UwMd411JTwQpDw
      - ELASTIC_SEARCH_URL=http://elastic:admin1234@elasticsearch_container:9200
      - ELASTIC_APM_SERVER_URL=http://localhost:8200
      - ELASTIC_APM_SECRET_TOKEN=
      - REDIS_HOST=redis://redis_container:6379
      - DATABASE_URL=mongodb://127.0.0.1:27017/fixme-users
    depends_on:
      - elasticsearch
      - redis
      - mongodb
      - rabbitmq
    networks:
      - elastic

  gig:
    container_name: gig_container
    build:
      context: ../server/5-gig-service
      dockerfile: Dockerfile.dev
    restart: always
    ports:
      - 4004:4004
    env_file: ../server/5-gig-service/.env
    environment:
      - ENABLE_APM=1
      - DATABASE_URL=mongodb://mongodb_container:27017/fixme-gig
      - GATEWAY_JWT_TOKEN=3f472800bcd6467b4d3b121d054e57e3
      - JWT_TOKEN=84f8e8de359784c77e032ceb2cb84df8
      - NODE_ENV=development
      - AP_GATEWAY_URL=http://gateway_container:4000
      - RABBITMQ_ENDPOINT=amqp://fixme:fixmepass@rabbitmq_container:5672
      - CLOUD_NAME=dwg9ek7pv
      - CLOUD_API_KEY=373746247683531
      - CLOUD_API_SECRET=-iQodxUen3OEQ3UwMd411JTwQpDw
      - REDIS_HOST=redis://redis_container:6379
      - ELASTIC_SEARCH_URL=http://elastic:admin1234@elasticsearch_container:9200
      - ELASTIC_APM_SERVER_URL=http://localhost:8200
      - ELASTIC_APM_SECRET_TOKEN=
    depends_on:
      - elasticsearch
      - redis
      - mongodb
      - rabbitmq
    networks:
      - elastic


networks:
  elastic:
    name: elastic
