apiVersion: v1
kind: Service
metadata:
  name: fixme-auth
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-auth
  ports:
  - name: auth
    port: 4002
    targetPort: 4002
    protocol: TCP
