apiVersion: v1
kind: Service
metadata:
  name: fixme-order
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-order
  ports:
  - name: order
    port: 4006
    targetPort: 4006
    protocol: TCP
