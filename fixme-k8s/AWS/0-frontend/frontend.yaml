apiVersion: apps/v1
kind: Deployment
metadata:
  name: fixme-frontend
  namespace: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fixme-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25% 
      maxSurge: 25%
  template:
    metadata:
      labels:
        app: fixme-frontend
    spec:
      containers:
      - name: fixme-frontend
        image: uzochukwueddie/fixme-frontend:stable
        imagePullPolicy: Always
        resources:
          limits:
            cpu: '0.5'
            memory: 1Gi
          requests:
            cpu: 100m
            memory: 150Mi
        ports:
        - name: frontend
          containerPort: 80

