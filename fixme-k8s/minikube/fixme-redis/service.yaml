apiVersion: v1
kind: Service
metadata:
  name: fixme-redis
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-redis
  ports:
  - name: fixme-redis
    port: 6379
    targetPort: 6379
