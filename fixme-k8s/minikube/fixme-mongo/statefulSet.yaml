apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: fixme-mongo
  namespace: production
spec:
  selector:
    matchLabels:
      app: fixme-mongo
  serviceName: fixme-mongo
  replicas: 1
  template:
    metadata:
      labels:
        app: fixme-mongo
    spec:
      containers:
      - name: fixme-mongo
        image: mongo:latest
        ports:
        - containerPort: 27017
          name: fixme-mongo
        volumeMounts:
        - name: mongo-local-storage
          mountPath: /data/db
      volumes:
      - name: mongo-local-storage
        persistentVolumeClaim:
          claimName: mongo-pvc