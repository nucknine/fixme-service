apiVersion: v1
kind: Service
metadata:
  name: fixme-kibana
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-kibana
  ports:
  - name: fixme-kibana
    port: 5601
    targetPort: 5601
    protocol: TCP
