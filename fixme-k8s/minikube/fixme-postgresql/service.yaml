apiVersion: v1
kind: Service
metadata:
  name: fixme-postgres
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-postgres
  ports:
  - name: fixme-postgres
    port: 5432
    targetPort: 5432
