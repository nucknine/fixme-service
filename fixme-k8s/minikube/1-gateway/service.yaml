apiVersion: v1
kind: Service
metadata:
  name: fixme-gateway
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-gateway
  ports:
  - name: gateway
    port: 4000
    targetPort: 4000
    protocol: TCP
