apiVersion: v1
kind: Service
metadata:
  name: fixme-gig
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-gig
  ports:
  - name: gig
    port: 4004
    targetPort: 4004
    protocol: TCP
