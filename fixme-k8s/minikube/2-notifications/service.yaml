apiVersion: v1
kind: Service
metadata:
  name: fixme-notification
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-notification
  ports:
  - name: notification
    port: 4001
    targetPort: 4001
    protocol: TCP
