apiVersion: v1
kind: Service
metadata:
  name: fixme-users
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fixme-users
  ports:
  - name: users
    port: 4003
    targetPort: 4003
    protocol: TCP
